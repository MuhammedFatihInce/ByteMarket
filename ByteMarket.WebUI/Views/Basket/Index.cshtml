@model List<ByteMarket.WebUI.Models.Basket.BasketItemViewModel>
@inject IConfiguration Configuration

@{
    ViewData["Title"] = "Alışveriş Sepetim";

    var apiBaseUrl = Configuration["ApiSettings:BaseStorageAdress"];
    string noImageUrl = "https://placehold.co/600x400?text=Resim+Yok";
}

<div class="container mt-5">
    <h2><i class="fa fa-shopping-cart"></i> @ViewData["Title"]</h2>
    <hr />


    @if (Model != null && Model.Any())
    {

        <div class="row">
            <div class="col-md-8">
                <div class="shadow-sm mt-4" style="border: 1px solid rgba(242, 122, 26, 0.1);">
                    <table class="table table-borderless table-hover table-sm mb-0">
                        <thead class="bg-white" style="border-bottom: 1px solid rgba(0, 0, 0, 0.15) !important;">
                            <tr>
                                <th class="py-4"></th>
                                <th></th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                var currentImagePath = !string.IsNullOrEmpty(item.ImagePath)
                                ? $"{apiBaseUrl}{item.ImagePath.Replace("\\", "/")}"
                                : noImageUrl;

                                <tr class="align-middle border-bottom">
                                    <td class="ps-4" style="width: 50%;">
                                        <div class="d-flex align-items-center gap-3">
                                            <div class="image-box-basket border rounded p-1">
                                                <img src="@currentImagePath"
                                                     class="img-fluid object-fit-contain h-100 w-100"
                                                     alt="@item.ProductName"
                                                     onerror="this.src='@noImageUrl'">
                                            </div>
                                            <span class="fw-bold">@item.ProductName</span>
                                        </div>
                                    </td>
                                    <td style="width: 50%;">
                                        <div class="input-group input-group-sm align-items-center bg-white rounded-pill p-1" style="border: 1px solid #ddd; width: 110px;">
                                            <button class="btn btn-white rounded-circle border-0 shadow-none px-2"
                                                    type="button"
                                                    onclick="changeQuantity('@item.BasketItemId', -1)">
                                                <i class="fa fa-minus" style="font-size: 0.8rem; color: light-dark(rgba(16, 16, 16, 0.3), rgba(255, 255, 255, 0.3))"></i>
                                            </button>

                                            <input type="text"
                                                   id="qty-@item.BasketItemId"
                                                   class="form-control border-0 text-center fw-bold shadow-none p-0"
                                                   style="background: #fff0e4; color: #f27a1a; border-radius: 50%; width: 24px; height: 24px; pointer-events: none;"
                                                   value="@item.Quantity" readonly>

                                            <button class="btn btn-white rounded-circle border-0 shadow-none px-2"
                                                    type="button"
                                                    onclick="changeQuantity('@item.BasketItemId', 1)">
                                                <i class="fa fa-plus text-ty-orange" style="font-size: 0.8rem;"></i>
                                            </button>
                                        </div>
                                    </td>
                                    <td class="ps-5">
                                        <div class="d-flex flex-column justify-content-start align-items-center h-100 py-2">
                                            <a href="/Basket/RemoveItem/@item.BasketItemId" class="text-black text-decoration-none border-0 p-0 mb-3 d-flex align-items-center gap-1">
                                                <i class="fa fa-trash small"></i>
                                                <small>Sil</small>
                                            </a>

                                            <span class="text-ty-orange fw-bold fs-5" style="margin-top: 50%">
                                                @item.Price.ToString("C2")
                                            </span>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="col-md-4">
                <div class="card border-0 shadow-sm rounded-4 sticky-top" style="top: 20px;">
                    <div class="card-body p-4">
                        <h5 class="fw-bold mb-4">Sepet Özeti</h5>
                        
                        <div class="d-flex justify-content-between mb-2">
                            <span class="text-muted">Ürün Toplamı</span>
                            <span class="fw-bold">@Model.Sum(x => x.Total).ToString("C2")</span>
                        </div>
                        
                        <div class="d-flex justify-content-between mb-3 text-success">
                            <span>Kargo Toplam</span>
                            <span class="fw-bold">Bedava</span>
                        </div>
                        
                        <hr />
                        
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <span class="h6 mb-0">Toplam</span>
                            <span class="h4 mb-0 fw-bold text-ty-orange">@Model.Sum(x => x.Total).ToString("C2")</span>
                        </div>
                        
                        <button class="btn btn-ty-orange text-white w-100 py-3 rounded-3 fw-bold shadow-sm">
                            Sepeti Onayla
                        </button>
                        
                        <div class="mt-3 text-center">
                            <a href="/" class="text-decoration-none small text-primary">
                                <i class="fa fa-arrow-left me-1"></i> Alışverişe Devam Et
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="alert alert-warning text-center">
            <h4>Sepetinizde ürün bulunmamaktadır.</h4>
            <a href="/" class="btn btn-primary mt-3">Ürünlere Göz At</a>
        </div>
    }
</div>


<script>
    function changeQuantity(itemId, amount) {
        // İlgili input elementini bul
        var input = document.getElementById('qty-' + itemId);
        var currentVal = parseInt(input.value);

        // Yeni değeri hesapla
        var newVal = currentVal + amount;

        // Miktar 1'den az olamaz
        if (newVal >= 1) {
            input.value = newVal;

            // BURASI ÖNEMLİ:
            // Burada sunucuya (Controller) sepeti güncellemesi için
            // bir istek (Ajax) göndermen gerekecek.
            // Şimdilik sadece görseli değiştiriyoruz.
            console.log("Ürün ID: " + itemId + " Yeni Miktar: " + newVal);

            // İstersen sayfayı yenileyerek güncel tutarları çekebilirsin:
            // window.location.href = '/Basket/UpdateQuantity?itemId=' + itemId + '&quantity=' + newVal;
        }
    }
</script>